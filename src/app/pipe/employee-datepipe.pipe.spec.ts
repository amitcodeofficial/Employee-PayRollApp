import { EmployeeDatepipePipe } from './employee-datepipe.pipe';

describe('EmployeeDatepipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeeDatepipePipe();
    expect(pipe).toBeTruthy();
  });
});
